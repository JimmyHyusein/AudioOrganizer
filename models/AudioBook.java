package NEW.model;

public class AudioBook extends AudioContent {

    public AudioBook(String title, String author, String genre, int year, int duration) {
        super(title, author, genre, year, duration);
    }
}
