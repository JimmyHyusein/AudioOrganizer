package NEW.model;

public class Song extends AudioContent {

    public Song(String title, String author, String genre, int year, int duration) {
        super(title, author, genre, year, duration);
    }
}
